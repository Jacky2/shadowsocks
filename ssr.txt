rpm -ivh http://dl.fedoraproject.org/pub/epel/epel-release-latest-7.noarch.rpm
rpm --import /etc/pki/rpm-gpg/RPM-GPG-KEY-EPEL-7
yum install m2crypto python-setuptools git lsof lsof lrzsz python-devel libffi-devel openssl-devel libsodium* && easy_install pip
pip install --upgrade pip setuptools
pip install cymysql
echo /usr/local/lib > /etc/ld.so.conf.d/usr_local_lib.conf && ldconfig


git clone -b manyuser https://github.com/Jacky2/shadowsocks.git

pip install -r requirements.txt

\\ 需要修改 userapiconfig.py user-config.json

	cp apiconfig.py userapiconfig.py && cp config.json user-config.json


设置开机自动运行

chmod +x *.sh && ./logrun.sh && ./tail.sh

  

  
运    行: ./logrun.sh 
查看日志：./tail.sh


网站后台，修改管理员 端口号 21000 , 重启 shutdown -r now ,

测试 可以使用了。